spring:
  application:
    name: employee-service
  profiles: 
    active: local
#  cloud:
#    config:
#      uri: http://localhost:8888


eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
    healthcheck:
      enabled: true